{
  "architecture": "MNISTExample",
  "dataset_name": "MNIST",
  "batch_size": 64,

  "optimizer": "Adam",
  "learning_rate": 0.1,
  "learning_rate_decay": 0.1,
  "num_epochs": 5,
  "num_epochs_decay": 1,

  "test_every": 1,
  "seed": 42
}
